import MemoryGame from "./MemoryGame";

export const metadata = {
  title: "Memory · BIntuitive",
  description: "Train memory by finding the pairs hidden on a vertical board.",
};

export default function MemoryGamePage() {
  return <MemoryGame />;
}
